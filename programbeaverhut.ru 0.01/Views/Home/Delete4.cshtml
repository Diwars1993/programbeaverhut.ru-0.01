@model programbeaverhut.ru.Models.ServiceName
@{
    ViewBag.Title = "Удаление услуги";
}

<div class="card border-primary" style="margin: 0px 0px 15px 0px;">

    <div class="card-header">
        <form class="form-row align-items-center ">
            <h4>Удаление услуги "@Html.DisplayFor(ServiceName => ServiceName.ServName)"</h4>
        </form>
    </div>

    <div class="card-body">

        <h6>Данные нельзя будет востоновить! Проверьте данные перед удалением.</h6>
        <div style="margin: 0px 0px 10px 0px;">
            <a>The data cannot be restored!</a>
        </div>

        <div>
            <form asp-controller="Home" asp-action="Delete4" method="post">
                <div class="form-group">
                    <input type="submit" class="form-group btn btn-outline-dark" value="Удалить" />
                </div>
            </form>
        </div>
    </div>
</div>
