@model programbeaverhut.ru.Models.ServiceName
@{
    ViewBag.Title = "Наименование услуги";
}

<div class="card border">

    <!-- card-header -->
    <div class="card-header">
        <form class="form-row align-items-center ">
            <h5>Редактировать Наименование услуги</h5>
        </form>
    </div>

    <div class="card-body">
        <form asp-action="edit12" asp-controller="home" asp-route-id="@Model.Id">

            <div class="form-group">
                <input type="hidden" asp-for="UserName" />
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="UserId1" />
            </div>

            <div class="form-group">
                <label asp-for="ServName" class="control-label">Наименование услуги</label>
                <input type="text" asp-for="ServName" class="form-control" />
            </div>

            <div class="form-group">
                <input type="submit" value="Сохранить" class="form-group btn btn-outline-dark" style="margin-bottom: 4px;" />
            </div>
        </form>
    </div>
</div>

