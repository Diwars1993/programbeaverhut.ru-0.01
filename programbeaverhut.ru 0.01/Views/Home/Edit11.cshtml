@model programbeaverhut.ru.Models.Category
@{
    ViewBag.Title = "Категории товара";
}

<div class="card border">

    <!-- card-header -->
    <div class="card-header">
        <form class="form-row align-items-center ">
            <h5>Редактировать Категории товара</h5>
        </form>
    </div>

    <div class="card-body">
        <form asp-action="edit11" asp-controller="home" asp-route-id="@Model.Id">

            <div class="form-group">
                <input type="hidden" asp-for="UserName" />
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="UserId1" />
            </div>

            <div class="form-group">
                <label asp-for="NameCategory" class="control-label">Наименование категории</label>
                <input type="text" asp-for="NameCategory" class="form-control" />
            </div>

            <div class="form-group">
                <input type="submit" value="Сохранить" class="form-group btn btn-outline-dark" style="margin-bottom: 4px;" />
            </div>
        </form>
    </div>
</div>

